{
  "name": "@chronam/core",
  "version": "0.1.0",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc -b",
    "clean": "rm -rf dist",
    "test": "vitest run"
  },
  "dependencies": {
    "@chronam/shared-types": "workspace:*",
    "@chronam/vhdl-parser": "workspace:*",
    "@chronam/testbench-generator": "workspace:*",
    "@chronam/simulation-engine": "workspace:*",
    "@chronam/vcd-parser": "workspace:*"
  },
  "devDependencies": {
    "typescript": "^5.5.0",
    "vitest": "^2.0.0"
  }
}
